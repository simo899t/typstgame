
1 + 1 = 2 # if you cant solve this, i suggest you drop out of university :/

a dot b + c dot d = e # dot

(a + b) dot (c + d) # no hint

x^2 + 2 x y + y^2 # ^

(x_(n+1) - x_n) / (x_n - x_(n-1)) # ()/()

sum_(i = 1)^n (i dot (i + 1)) # sum_a^b

product_(i = 1)^n (1 + 1 / i) # product_a^b

integral_0^1 (x^2 + x) space dif x # integral f space dif x

lim_(n -> oo) sum_(i = 1)^n 1 / n # lim_(x -> a) f

f(x) = x dot x + 2 dot x + 1 # no hint

f(x) = (x + 1)^2 # no hint

g(x) = x^2 + 1 quad f(x) = g(g(x)) # quad

x_(n+1) = x_n + 1 / x_n # _ /

x_(n+1) = x_n + 1 / (x_n + x_(n-1)) # () _

A = {x in RR | x^2 + x - 2 < 0} # RR, bar

A = {x in RR | (x - 1)(x + 2) < 0} # no hint

exists x in RR : x^2 - 2 dot x + 1 = 0 # exists

exists! x in RR : x dot x = 1 # EE! # no hint

forall x in RR : x^2 >= 0 # no hint

p -> (q -> r) # -> 

(p and q) -> (r or s) # and, or

not (p and (q or r)) # not

cases(x^2 + 1, x < 0,x dot x + x, x >= 0) # cases(,;)

mat(1, 2, 3;4, 5, 6;7, 8, 9) dot vec(x, y, z) # mat(,;) vec(,)

A^(-1) dot (A dot x) = x # ^(-1) ()

det(A dot B) = det(A) dot det(B) # det()

integral_0^1 (integral_0^x t dt) dx # integral integral

lim_(n -> oo) (1 + 1 / n)^n # no hint

sum_(i = 1)^n sum_(j = 1)^i (i dot j) # sum sum

f(x) = x dot x + sum_(i = 1)^n x_i / i # no hint

x = f(x dot x + sum_(i = 1)^n 1 / i) # no hint
11 + 1 = 2 # if you cant do this, i suggest you drop out of university.

a^2 + b^2 = c^2 # use `^` to superscript.

x = {x_1, x_2, x_3, dots, x_n} # use `_` to subscript. Use `dots` to dot.

v dot u = v_1 dot u_1 + v_2 dot u_2 # use `dot` to multiply (in contrast to \cdot).

e^(pi i) - 1 = 0 # use parentheses `()` and <space> between symbols to distinct between them.

y = f(x), quad x = f^(-1)(y) # Use `quad` to create 4 spaces (as opposed to \quad)

(p or q) and (not p or not q) # use keywords like `or` as operators for propositional logic

forall x in NN, exists y in NN : y > x # For blackboard bold symbols either use `bb(x)` or for common symbols like real numbers do `RR` (in contrast to \mathbb{R})

sum_(i=1)^5 i = 15 # Use `sum_a^b` to make sums initialized at from a to b

product_(i=1)^5 i = 120 # Use `product_a^b` to make products initialized at from a to b

integral_0^1 x space dif x = 1/2 # Use `dif x` to make products initialized at from a to b. 

EE[X in cal(X)] = mu # For calligraphy font use cal(x)

sum_(T in cal(T)) sum_(a in A) sum_(b in L_a) w(a) y_(T,a,b) # Use cal(T)

min_x c^T x \ s.t quad A^T x >= b # use `\` to break lines

